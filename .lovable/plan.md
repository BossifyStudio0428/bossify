我会按这个范围一次改完：

1. 首页启动清单
- 房地产类型不再显示“添加第一个配套”。
- 改成“添加第一个房源”。
- 点击后去 `/listings`，不是 `/services`。
- 完成状态检查 `listings` 表，不再检查 `services`。
- 实时刷新也监听 `listings`。

2. 底部和页面标签
- 房地产类型里原本映射到 `bl_packages` 的地方改成房源标签。
- 新增/使用 `bl_listings` 翻译：英文 Listings，马来文 Hartanah，中文 房源。
- 保留“配套/套餐”翻译给其他业务用，不删除。

3. 新建订单/客户连接
- 房地产的新建页面不再把房源当“服务”下拉。
- 房源下拉直接读取 `listings` 表，并显示你已经添加的房源。
- 客户下拉读取 `customers` 表；选择客户后自动带入名字和电话。
- 如果没有房源，提示去“房源”页面添加，而不是去“服务”。

4. 旧表名连接修正
- 把还在读 `property_listings` 的页面改读现在真实存在的 `listings` 表，例如：看房、佣金、文件、首页今日看房等。
- 这样新增的房源会在相关 dropdown/list 里面出现。

5. 后端 SQL
- 我会给你一段 SQL，用来补齐房地产相关表：看房、佣金、客户需求、续约提醒、文件清单。
- SQL 会包含权限和用户隔离规则，避免表创建了但 app 读不到。
- `listings` 已存在，所以不会重复创建，只会补齐缺少的关联字段/索引。